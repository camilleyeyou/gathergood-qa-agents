---
phase: 1
slug: foundation
status: draft
shadcn_initialized: true
preset: none
created: 2026-03-28
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | none (manual init — Tailwind v4 CSS-native, no tailwind.config.js) |
| Component library | Radix UI (via shadcn primitives) |
| Icon library | lucide-react |
| Font | Inter via next/font/google |

**Source:** All values from user decisions (2026-03-28).

**Tailwind v4 note:** Zero-config setup. Import via `@import "tailwindcss"` in global CSS. CSS custom properties defined in `:root` under `@layer base`. No `tailwind.config.js` required. shadcn tokens declared as CSS variables, referenced with `hsl(var(--token))` syntax.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding, tight label spacing |
| sm | 8px | Input internal padding (vertical), button icon gap, form field hint spacing |
| md | 16px | Default element spacing, card internal padding, input horizontal padding |
| lg | 24px | Section padding, card-to-card gaps, dialog content padding |
| xl | 32px | Layout column gaps, form section breaks |
| 2xl | 48px | Major section breaks between page zones |
| 3xl | 64px | Page-level top/bottom padding on auth screens |

Exceptions:
- Touch targets on all interactive controls: minimum 44x44px (per FEND-08 — Phase 6 enforces globally, but Phase 1 components must be built to this floor)
- Role badge chips: 4px vertical + 8px horizontal padding (xs/sm)
- Avatar in navbar: 32x32px displayed, 44x44px tap target via padding

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 (regular) | 1.5 | Form field labels, paragraph text, list items, table cell content |
| Label | 14px | 400 (regular) | 1.4 | Input labels, helper text, table column headers, nav links, badge text |
| Heading | 20px | 600 (semibold) | 1.3 | Page titles, card headings, dialog titles, section headers |
| Display | 28px | 600 (semibold) | 1.2 | Auth screen titles (/register, /login, /forgot-password) only |

**Font family:** Inter (next/font/google — subsets: latin, variable font)

**Weights declared:** 400, 600 — loaded via `next/font` weight array `['400', '600']` to avoid FOUT.

**Notes:**
- Body and Label share Inter — only size differs; both use weight 400
- Heading and Display both use weight 600; Display is reserved for the four auth screens only — do not use on manage/ routes
- Monospace exception: confirmation codes rendered in `font-mono` (system stack) at 20px weight 600

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #FFFFFF / hsl(0 0% 100%) | Page background, form backgrounds, auth card fill |
| Secondary (30%) | #F9FAFB / hsl(220 14% 96%) | Manage dashboard sidebar, data table alternating rows, input fill on focus-free state, nav background |
| Accent (10%) | #0D9488 / hsl(173 80% 31%) | Reserved — see list below |
| Destructive | #DC2626 / hsl(0 72% 47%) | Remove member button, destructive confirmation dialogs only |

**Accent (#0D9488) reserved for:**
1. Primary CTA buttons (Register, Log In, Save Changes, Create Organization, Create Venue, Invite Member)
2. Active navigation link indicator (left border or underline on current route)
3. Focus rings on all interactive elements (2px ring, 2px offset)
4. Role badge chip background for OWNER role
5. Link color in body text (hover: teal-700 #0F766E)

**Accent NOT used for:** secondary buttons, ghost buttons, table row hover, card borders, input borders (default state).

**Dark mode:** Out of scope for Phase 1. shadcn tokens defined in `:root` only.

**Semantic color intent mapping (shadcn CSS variables):**

| Variable | Value | Maps To |
|----------|-------|---------|
| `--background` | 0 0% 100% | Dominant |
| `--foreground` | 222 47% 11% | Body text (near-black) |
| `--muted` | 220 14% 96% | Secondary surface |
| `--muted-foreground` | 215 16% 47% | Helper text, placeholder |
| `--primary` | 173 80% 31% | Accent (#0D9488) |
| `--primary-foreground` | 0 0% 100% | Text on accent buttons |
| `--destructive` | 0 72% 47% | Remove / delete actions |
| `--destructive-foreground` | 0 0% 100% | Text on destructive buttons |
| `--border` | 220 13% 91% | Input borders, card borders, dividers |
| `--ring` | 173 80% 31% | Focus ring (matches primary) |

---

## Component Inventory

Components required in Phase 1 (sourced from shadcn official registry only):

| Component | shadcn command | Used On |
|-----------|---------------|---------|
| Button | `npx shadcn add button` | All screens — primary, secondary, ghost variants |
| Input | `npx shadcn add input` | All forms — email, password, text fields |
| Label | `npx shadcn add label` | All form fields |
| Form | `npx shadcn add form` | react-hook-form integration wrapper |
| Card | `npx shadcn add card` | Auth screen containers, org list items, venue list items |
| Dialog | `npx shadcn add dialog` | Invite member modal, remove member confirmation, create/edit venue inline |
| Table | `npx shadcn add table` | Team member list, venue list |
| Badge | `npx shadcn add badge` | Role chips (OWNER, MANAGER, VOLUNTEER) |
| Avatar | `npx shadcn add avatar` | Navbar user avatar, profile settings |
| Separator | `npx shadcn add separator` | Navbar divider, form section breaks |
| Toast / Sonner | `npx shadcn add sonner` | Success/error feedback on form submit, invite sent, member removed |
| Dropdown Menu | `npx shadcn add dropdown-menu` | Navbar avatar dropdown (settings, logout) |
| Alert | `npx shadcn add alert` | Inline form-level error messages (e.g. "Email already registered") |
| Skeleton | `npx shadcn add skeleton` | Loading states for org list, member list, venue list |

---

## Screen-by-Screen Interaction Contract

### /register

**Focal point:** The "Create account" button (accent fill, full-width) is the visual anchor. All form fields lead the eye downward to this single CTA. No competing actions above the fold.

**Visual hierarchy:** Display title (28px/600) establishes page identity → Body fields (16px/400) collect information → Full-width accent CTA closes the flow. Secondary link ("Already have an account?") sits below the CTA at Label size (14px/400) in muted color.

**Layout:** Centered single-column card. Max-width 400px. Vertical padding: 64px (3xl) top/bottom on outer container.

**Fields:** First Name, Last Name, Email, Password, Confirm Password.

**Interactions:**
- All fields validated on blur via Zod + react-hook-form
- Password field: show/hide toggle (Eye icon from lucide-react, 20px, muted color; `aria-label="Show password"` when hidden, `aria-label="Hide password"` when visible)
- Submit button: full-width, accent fill, label "Create account"
- On submit: button enters loading state (spinner replaces label, button disabled)
- On API error (400 email exists): inline Alert component below email field — "An account with this email already exists."
- On success: redirect to /manage (no success toast — navigation is the confirmation)
- Link below form: "Already have an account? Log in" — links to /login

**States required:** idle, loading (submitting), field-error, form-error (API).

---

### /login

**Focal point:** The "Log in" button (accent fill, full-width) is the visual anchor. Two fields and one CTA — no visual clutter.

**Visual hierarchy:** Display title (28px/600) → Email and Password fields (16px/400) → Full-width accent CTA → Secondary links (14px/400, muted) for forgot password and register.

**Layout:** Centered single-column card. Max-width 400px. Vertical padding: 64px top/bottom.

**Fields:** Email, Password.

**Interactions:**
- Password field: show/hide toggle (`aria-label="Show password"` when hidden, `aria-label="Hide password"` when visible)
- Submit button: full-width, accent fill, label "Log in"
- On submit: loading state
- On API error (401): inline Alert below password field — "Incorrect email or password."
- On success: redirect to /manage
- Link below form: "Forgot your password? Reset it" — links to /forgot-password
- Link below form: "Don't have an account? Sign up" — links to /register

**States required:** idle, loading, field-error, form-error (API).

---

### /forgot-password

**Focal point:** The "Send reset link" button (accent fill, full-width) is the visual anchor. Single email field minimizes friction.

**Visual hierarchy:** Display title (28px/600) → Single email field (16px/400) → Full-width accent CTA → "Back to login" link (14px/400, muted) at card top as secondary escape.

**Layout:** Centered single-column card. Max-width 400px. Vertical padding: 64px.

**Fields:** Email.

**Interactions:**
- Submit button: full-width, accent fill, label "Send reset link"
- On success: replace form with success message — "Check your email. If an account exists for [email], you'll receive a password reset link." Do not reveal whether the email exists.
- On submit: loading state
- Back link: "Back to login" at top of card

**States required:** idle, loading, success (form replaced — no field errors exposed).

---

### /reset-password

**Focal point:** The "Reset password" button (accent fill, full-width) is the visual anchor. Two password fields with show/hide toggles are the only interactive elements.

**Visual hierarchy:** Display title (28px/600) → Two password fields (16px/400) → Full-width accent CTA. Expired-token Alert appears above the form at the same visual weight as field errors.

**Layout:** Centered single-column card. Max-width 400px. Vertical padding: 64px.

**Fields:** New Password, Confirm New Password.

**Interactions:**
- Both fields: show/hide toggle (`aria-label="Show password"` when hidden, `aria-label="Hide password"` when visible)
- Submit button: full-width, accent fill, label "Reset password"
- On submit: loading state
- On API error (invalid/expired token): Alert — "This reset link has expired or is invalid. Request a new one." with link to /forgot-password
- On success: redirect to /login with sonner toast — "Password updated. Please log in."

**States required:** idle, loading, field-error, expired-token error, success (redirect).

---

### /my/settings

**Focal point:** The "Save changes" button (accent fill, right-aligned) is the visual anchor for each section. Avatar preview provides secondary visual interest when a URL is supplied.

**Visual hierarchy:** Section Heading (20px/600) labels each group → form fields (16px/400) below → right-aligned accent CTA closes each section. Read-only email field rendered at lower visual weight (muted foreground color) to signal non-editability.

**Layout:** Single-column content area, max-width 640px, left-aligned within manage layout shell.

**Sections:**
1. Profile section — First Name, Last Name, Phone (optional), Avatar URL (optional). Save button: "Save changes" (accent fill, right-aligned).
2. Account section — Email displayed read-only (non-editable in Phase 1).

**Interactions:**
- Form validates on blur
- Save button: loading state on submit
- On success: sonner toast — "Profile updated."
- On API error: Alert above form — "Could not save changes. Please try again."
- Avatar URL field: renders preview thumbnail (32x32px) inline if URL is non-empty and valid

**States required:** idle, loading (saving), success (toast), error (alert).

---

### /manage (Organizer Dashboard — Org List)

**Focal point:** In empty state, the "Create organization" CTA (accent fill) is the visual anchor, centered in the content area. In populated state, the "New organization" button (top-right, accent fill) anchors the header; org cards draw the eye with name as the dominant text element.

**Visual hierarchy:** Page heading (20px/600) → card grid with org name as Heading (20px/600) per card, slug and member count as Label (14px/400, muted) below. Empty state heading (20px/600) → body copy (16px/400) → centered CTA.

**Layout:** Manage layout shell (sidebar nav + content area). Content area: list of org cards.

**Empty state:**
- Heading: "No organizations yet"
- Body: "Create your first organization to start managing events and venues."
- CTA button: "Create organization" (accent fill, links to /manage/org/new)

**Populated state:**
- Each org rendered as a Card with: org name (heading), slug (label, muted), member count (label). Clicking card navigates to /manage/org/{slug}.
- "New organization" button in top-right of content area header (accent fill).

**Loading state:** 3 Skeleton cards (same card dimensions as real cards).

**States required:** loading, empty, populated.

---

### /manage/org/new

**Focal point:** The "Create organization" button (accent fill) is the visual anchor at the base of the single-field form.

**Visual hierarchy:** Page heading (20px/600) → Organization Name field (16px/400) → slug preview in Label (14px/400, muted) → full-width accent CTA → "Back to organizations" cancel link (14px/400, muted).

**Layout:** Single-column form, max-width 640px.

**Fields:** Organization Name (required). Slug shown as read-only preview below name field, updated in real-time as user types — format: `gathergood.com/org/{slug-preview}` in muted Label text.

**Interactions:**
- Submit button: full-width within form, accent fill, label "Create organization"
- On submit: loading state
- On API error (slug conflict): Alert — "An organization with this name already exists. Try a more specific name."
- On success: redirect to /manage/org/{slug}
- Cancel link: "Back to organizations" — navigates to /manage

**States required:** idle, loading, error (API), success (redirect).

---

### /manage/org/{slug}

**Focal point:** The "Save changes" button (accent fill, right-aligned) anchors the org details form. Sub-navigation tabs below the org heading draw secondary attention.

**Visual hierarchy:** Org name as Heading (20px/600) → editable fields (16px/400) → right-aligned accent CTA → tab nav (Label 14px/400) separating Team and Venues sub-pages. VOLUNTEER read-only state: disabled inputs render at lower visual weight to communicate non-editability without confusion.

**Layout:** Two-section page within manage shell. Top section: org details form. Below: navigation tabs or links to Team and Venues sub-pages.

**Fields:** Organization Name (required).

**Interactions:**
- Save button: "Save changes" (accent fill)
- On submit: loading state
- On success: sonner toast — "Organization updated."
- Sub-navigation: links to /manage/org/{slug}/team and /manage/org/{slug}/venues rendered as pill tabs or underline tabs below org name heading.

**Permission enforcement:** VOLUNTEER role sees form fields read-only (disabled inputs). Save button hidden for VOLUNTEER.

**States required:** idle, loading, success (toast), error (alert), read-only (VOLUNTEER).

---

### /manage/org/{slug}/team

**Focal point:** The "Invite member" button (accent fill, top-right of content header) is the primary visual anchor. Role badges provide secondary color interest to guide the eye across table rows.

**Visual hierarchy:** "Team" Heading (20px/600) + right-aligned accent CTA in the header row → table column headers as Label (14px/400) → member name as Body (16px/400) dominant per row, role badge and action button as secondary elements. Remove button (ghost, destructive color) is intentionally low-contrast to prevent accidental activation.

**Layout:** Content area within manage shell. Header row: "Team" heading + "Invite member" button (accent fill, right-aligned).

**Table columns:** Name, Email, Role (Badge chip), Actions.

**Role badge colors:**
- OWNER: accent fill (#0D9488), white text
- MANAGER: amber-100 background, amber-800 text (`bg-amber-100 text-amber-800`)
- VOLUNTEER: slate-100 background, slate-700 text (`bg-slate-100 text-slate-700`)

**Invite member:**
- Opens Dialog. Fields: Email (required), Role (Select: MANAGER or VOLUNTEER). MANAGER cannot see OWNER as an option (enforced client-side + server-side per TEAM-02).
- Dialog submit button: "Send invite" (accent fill)
- On success: Dialog closes, sonner toast — "Invite sent to [email]."
- On API error: inline Alert inside Dialog — "Could not send invite. Check the email and try again."

**Remove member:**
- Only rendered for OWNER role users
- "Remove" button (ghost variant, destructive text color) in Actions column
- Clicking opens confirm Dialog — "Remove [Name] from this organization? This cannot be undone." with "Remove member" button (destructive fill) and "Cancel" (ghost)
- On confirm: loading state on button, member removed from table, sonner toast — "Member removed."

**Empty state (no members):**
- Text: "No team members yet." (will not display in practice — OWNER always present)

**Loading state:** Skeleton rows matching table structure.

**States required:** loading, populated, invite-dialog (idle/loading/error), remove-confirm-dialog (idle/loading), error.

---

### /manage/org/{slug}/venues

**Focal point:** The "Add venue" button (accent fill, top-right of content header) is the primary visual anchor in all states. In empty state, the inline "Add venue" CTA (centered) is the sole focal point.

**Visual hierarchy:** "Venues" Heading (20px/600) + right-aligned accent CTA in the header row → table column headers as Label (14px/400) → venue name as Body (16px/400) dominant per row. Empty state: Heading (20px/600) → body copy (16px/400) → centered accent CTA.

**Layout:** Content area within manage shell. Header: "Venues" heading + "Add venue" button (accent fill).

**Table columns:** Name, Address, City, Capacity, Actions (Edit).

**Add / Edit venue:**
- Opens Dialog (used for both create and update).
- Fields: Name (required), Address (required), City (required), State (required), Postal Code (required), Capacity (number, required), Accessibility Info (textarea, optional), Parking Notes (textarea, optional).
- Dialog title: "Add venue" (create) or "Edit venue" (update).
- Submit button: "Save venue" (accent fill).
- On success: Dialog closes, sonner toast — "Venue saved." Table refreshes.
- On API error: Alert inside Dialog — "Could not save venue. Please try again."

**Empty state:**
- Heading: "No venues yet"
- Body: "Add your first venue to reuse it across events."
- CTA: "Add venue" button (accent fill)

**Loading state:** Skeleton rows.

**States required:** loading, empty, populated, venue-dialog (idle/loading/error).

---

### Navbar (Auth-Aware)

**Focal point:** In logged-out state, the "Sign up" button (accent fill, right-aligned) is the visual anchor. In logged-in state, the GatherGood wordmark (accent color) anchors the left; the Avatar dropdown trigger anchors the right.

**Visual hierarchy (desktop):** Wordmark (Inter 600, 18px, accent) → center nav links (Label 14px/400) with active route indicated by 2px accent bottom border → Avatar trigger (right) as secondary CTA. Mobile: Wordmark → hamburger icon (right-aligned, 24px) as the sole secondary element, replacing all center nav.

**Icon-only accessible labels:**
- Password show/hide Eye icon: `aria-label="Show password"` (when password hidden) / `aria-label="Hide password"` (when password visible)
- Mobile hamburger Menu icon: `aria-label="Open navigation menu"` (when closed) / `aria-label="Close navigation menu"` (when sheet is open)

**Layout:** Full-width top bar. Height: 56px. Background: secondary surface (#F9FAFB). Bottom border: 1px solid `--border`. Sticky at top.

**Logged-out state:**
- Left: GatherGood wordmark (Inter 600, 18px, accent color)
- Right: "Log in" (ghost button) + "Sign up" (accent fill button)

**Logged-in state:**
- Left: GatherGood wordmark
- Center (desktop): "Dashboard" link (/manage), "Browse" link (placeholder, Phase 2), "My Tickets" link (placeholder, Phase 3)
- Right: Avatar (32x32px, with initials fallback) in Dropdown Menu trigger
  - Dropdown items: "Settings" → /my/settings, divider, "Log out" (triggers token clear + redirect to /login)

**Mobile (< 768px):**
- Hamburger menu button (Menu icon from lucide-react, 24px; `aria-label="Open navigation menu"`) replaces center nav
- Drawer or sheet opens with all nav links stacked vertically (per FEND-03); button `aria-label` updates to `"Close navigation menu"` while sheet is open
- Use shadcn Sheet component (`npx shadcn add sheet`) for mobile nav drawer

**Active link indicator:** 2px bottom border in accent color (#0D9488) on current route link (desktop). Accent text color on current route in mobile drawer.

**States required:** logged-out, logged-in (desktop), logged-in (mobile — collapsed), mobile-menu-open.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — register | "Create account" |
| Primary CTA — login | "Log in" |
| Primary CTA — forgot password | "Send reset link" |
| Primary CTA — reset password | "Reset password" |
| Primary CTA — create org | "Create organization" |
| Primary CTA — invite member | "Send invite" |
| Primary CTA — save profile/org/venue | "Save changes" |
| Primary CTA — save venue (dialog) | "Save venue" |
| Empty state — org list heading | "No organizations yet" |
| Empty state — org list body | "Create your first organization to start managing events and venues." |
| Empty state — venue list heading | "No venues yet" |
| Empty state — venue list body | "Add your first venue to reuse it across events." |
| Error — email already exists | "An account with this email already exists." |
| Error — wrong credentials | "Incorrect email or password." |
| Error — expired reset token | "This reset link has expired or is invalid. Request a new one." |
| Error — generic form save | "Could not save changes. Please try again." |
| Error — invite failed | "Could not send invite. Check the email and try again." |
| Error — venue save failed | "Could not save venue. Please try again." |
| Forgot password success | "Check your email. If an account exists for this address, you'll receive a password reset link." |
| Password reset success (toast) | "Password updated. Please log in." |
| Profile updated (toast) | "Profile updated." |
| Org updated (toast) | "Organization updated." |
| Invite sent (toast) | "Invite sent to [email]." |
| Venue saved (toast) | "Venue saved." |
| Member removed (toast) | "Member removed." |
| Remove member — confirm dialog heading | "Remove [Name]?" |
| Remove member — confirm dialog body | "This will remove [Name] from this organization. This cannot be undone." |
| Remove member — confirm button | "Remove member" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (ui.shadcn.com) | button, input, label, form, card, dialog, table, badge, avatar, separator, sonner, dropdown-menu, alert, skeleton, sheet | not required — official registry, no third-party code |

No third-party registries declared for Phase 1.

---

## Pre-Population Sources

| Section | Source |
|---------|--------|
| Design system (shadcn + Tailwind v4 + Inter + #0D9488) | User decisions (2026-03-28) |
| Accent reserved-for list | User decisions (2026-03-28) |
| Screen inventory | Phase scope definition (phase_scope) |
| Permission enforcement (OWNER/MANAGER/VOLUNTEER) | REQUIREMENTS.md TEAM-01 through TEAM-04 |
| FEND-02, FEND-03 (navbar spec) | REQUIREMENTS.md |
| Touch target floor (44px) | REQUIREMENTS.md FEND-08 |
| Role badge behavior | REQUIREMENTS.md TEAM-02 |
| Empty/error/destructive copy | Derived from requirement success criteria |
| Component selection | STACK.md (react-hook-form, lucide-react, shadcn) |
| Typography weight reduction (4 → 2) | Checker revision 2026-03-28 |
| Focal point + visual hierarchy statements | Checker revision 2026-03-28 |
| aria-label specifications (Eye, hamburger) | Checker revision 2026-03-28 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
