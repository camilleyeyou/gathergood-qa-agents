---
phase: 07-digital-literacy-persona-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/persona_agents/__init__.py
  - tests/persona_agents/personas.py
  - tests/persona_agents/friction_scorer.py
  - tests/persona_agents/persona_runner.py
  - tests/persona_agents/artifact_writer.py
autonomous: true
requirements:
  - P7-SC1
  - P7-SC2
  - P7-SC5

must_haves:
  truths:
    - "5 persona dataclasses are importable with name, literacy_level, system_prompt, constraints fields"
    - "Friction scorer returns integer 1-10 based on step ratio, confusion severity, and task completion"
    - "Persona runner wraps run_agent_scenario() with persona system prompt and parses structured JSON output"
    - "Artifact writer saves per-persona-per-flow JSON to reports/persona/<run_id>/ directory"
  artifacts:
    - path: "tests/persona_agents/personas.py"
      provides: "5 persona dataclass definitions and OPTIMAL_STEPS dict"
      exports: ["Persona", "TECH_SAVVY", "CASUAL", "LOW_LITERACY", "NON_NATIVE", "IMPATIENT", "ALL_PERSONAS", "OPTIMAL_STEPS"]
    - path: "tests/persona_agents/friction_scorer.py"
      provides: "Friction score calculation per D-05"
      exports: ["calculate_friction_score"]
    - path: "tests/persona_agents/persona_runner.py"
      provides: "Persona-aware agent scenario wrapper"
      exports: ["run_persona_scenario"]
    - path: "tests/persona_agents/artifact_writer.py"
      provides: "JSON result file writer"
      exports: ["save_persona_result"]
  key_links:
    - from: "tests/persona_agents/persona_runner.py"
      to: "tests/ai_agents/agent_runner.py"
      via: "import run_agent_scenario"
      pattern: "from tests\\.ai_agents\\.agent_runner import run_agent_scenario"
    - from: "tests/persona_agents/persona_runner.py"
      to: "tests/persona_agents/friction_scorer.py"
      via: "import calculate_friction_score"
      pattern: "from tests\\.persona_agents\\.friction_scorer import calculate_friction_score"
---

<objective>
Build the core persona agent library: persona definitions, friction scoring, persona-aware agent runner wrapper, and JSON artifact writer.

Purpose: These modules form the foundation that all persona tests, the report generator, and the FastAPI endpoint import from. No new packages needed — all dependencies already installed.

Output: 5 importable Python modules in tests/persona_agents/ that enable running any persona through any flow and saving structured results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-digital-literacy-persona-agents/07-CONTEXT.md
@.planning/phases/07-digital-literacy-persona-agents/07-RESEARCH.md

@tests/ai_agents/agent_runner.py
@tests/ai_agents/computer_use_backend.py
@tests/ai_agents/conftest.py
@settings.py

<interfaces>
<!-- Existing agent framework contracts the persona runner must use -->

From tests/ai_agents/agent_runner.py:
```python
def run_agent_scenario(
    backend: PlaywrightComputerBackend,
    client: anthropic.Anthropic,
    goal: str,
    system_prompt: str | None = None,
    max_iterations: int = MAX_ITERATIONS,
    max_input_tokens: int = MAX_INPUT_TOKENS,
) -> dict[str, Any]:
    # Returns: {"verdict": str, "reasoning": str, "steps": int, "input_tokens": int, "output_tokens": int}
```

From tests/ai_agents/computer_use_backend.py:
```python
class PlaywrightComputerBackend:
    def __init__(self, page: Page): ...
    def screenshot(self) -> str: ...
    def execute_action(self, action: str, params: dict) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create persona definitions and friction scorer</name>
  <files>
    tests/persona_agents/__init__.py
    tests/persona_agents/personas.py
    tests/persona_agents/friction_scorer.py
  </files>
  <read_first>
    tests/ai_agents/agent_runner.py
    tests/ai_agents/conftest.py
    .planning/phases/07-digital-literacy-persona-agents/07-CONTEXT.md
    .planning/phases/07-digital-literacy-persona-agents/07-RESEARCH.md
  </read_first>
  <action>
Create `tests/persona_agents/__init__.py` as empty package marker.

Create `tests/persona_agents/personas.py` per D-01 and D-02:
- Define `@dataclass` class `Persona` with fields: `name: str`, `literacy_level: int` (1-5), `system_prompt: str`, `constraints: list[str] = field(default_factory=list)`
- Define all 5 persona instances as module-level constants:
  1. `TECH_SAVVY` — literacy_level=5, system_prompt uses first-person present tense: "I am a 25-year-old software developer..." with constraints: ["Use keyboard shortcuts when available (Tab, Enter, Ctrl+Click)", "Scan, don't read every word", "Click the most obvious primary action immediately"]
  2. `CASUAL` — literacy_level=3, "I am a 45-year-old office worker who uses websites occasionally..." with constraints: ["Read labels before clicking", "Hesitate at technical jargon", "Expect confirmation before important actions"]
  3. `LOW_LITERACY` — literacy_level=1, "I am a 70-year-old retiree who rarely uses websites. I read every word on the page before I click anything. I am confused when I see an icon without a text label next to it. I do not scroll down unless I see a scroll bar or arrow. I never use keyboard shortcuts. If I am unsure about a button, I describe my confusion rather than clicking it." with constraints: ["Never use keyboard shortcuts", "Only click elements with visible text labels", "Do not scroll unless you see a visual indicator", "Report confusion if any label is ambiguous or icon-only"]
  4. `NON_NATIVE` — literacy_level=3, "I am a non-native English speaker. I struggle with idioms, abbreviations, and jargon. I rely on visual cues, icons, and layout patterns more than text..." with constraints: ["Ignore idiomatic text; focus on icons and visual hierarchy", "Report confusion on any abbreviation or jargon", "Prefer visual navigation over text-based search"]
  5. `IMPATIENT` — literacy_level=4, "I am a busy, impatient user. I skip instructions, click fast, and abandon tasks if I'm stuck for more than a few seconds..." with constraints: ["Never read instructions or help text", "Click the first thing that looks relevant", "If stuck for more than 2 actions without progress, give up"]
- All system prompts MUST use first-person present tense per RESEARCH.md Pitfall 1
- All system prompts MUST end with: "When I complete the task or give up, I output my verdict (PASS/FAIL/INCONCLUSIVE) and the required JSON block describing my experience. Stay in character throughout."
- Define `ALL_PERSONAS = [TECH_SAVVY, CASUAL, LOW_LITERACY, NON_NATIVE, IMPATIENT]`
- Define `OPTIMAL_STEPS = {"registration": 6, "browsing": 3, "checkout": 5}`

Create `tests/persona_agents/friction_scorer.py` per D-05:
- Function `calculate_friction_score(steps_taken: int, optimal_steps: int, confusion_points: list[dict], task_completed: bool) -> int`
- If not task_completed: return 10 immediately
- Step ratio component: `ratio = min(steps_taken / max(optimal_steps, 1), 4.0)`, then `step_score = max(0.0, (ratio - 1.0) / 3.0 * 4.0)` — maps 1x=0pts, 4x=4pts
- Confusion component: severity_weights = {"low": 0.5, "medium": 1.0, "high": 2.0}, sum weights capped at 4.0
- Final: `raw = 1.0 + step_score + confusion_score`, return `min(10, max(1, round(raw)))`
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python -c "
from tests.persona_agents.personas import Persona, TECH_SAVVY, CASUAL, LOW_LITERACY, NON_NATIVE, IMPATIENT, ALL_PERSONAS, OPTIMAL_STEPS
assert len(ALL_PERSONAS) == 5
assert TECH_SAVVY.literacy_level == 5
assert LOW_LITERACY.literacy_level == 1
assert 'registration' in OPTIMAL_STEPS
from tests.persona_agents.friction_scorer import calculate_friction_score
assert calculate_friction_score(6, 6, [], True) == 1
assert calculate_friction_score(6, 6, [], False) == 10
assert calculate_friction_score(24, 6, [], True) == 5
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - tests/persona_agents/__init__.py exists (empty file)
    - tests/persona_agents/personas.py contains `class Persona:` and `ALL_PERSONAS = [`
    - tests/persona_agents/personas.py contains all 5 names: TECH_SAVVY, CASUAL, LOW_LITERACY, NON_NATIVE, IMPATIENT
    - tests/persona_agents/personas.py contains `OPTIMAL_STEPS = {`
    - Each persona system_prompt starts with "I am" (first-person present tense)
    - tests/persona_agents/friction_scorer.py contains `def calculate_friction_score(`
    - calculate_friction_score(6, 6, [], True) returns 1
    - calculate_friction_score(anything, anything, anything, False) returns 10
  </acceptance_criteria>
  <done>5 personas importable as dataclasses, friction scorer returns correct scores for edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Create persona runner wrapper and artifact writer</name>
  <files>
    tests/persona_agents/persona_runner.py
    tests/persona_agents/artifact_writer.py
  </files>
  <read_first>
    tests/ai_agents/agent_runner.py
    tests/persona_agents/personas.py
    tests/persona_agents/friction_scorer.py
    .planning/phases/07-digital-literacy-persona-agents/07-RESEARCH.md
  </read_first>
  <action>
Create `tests/persona_agents/persona_runner.py` per RESEARCH Pattern 2:
- Import `run_agent_scenario` from `tests.ai_agents.agent_runner`
- Import `PlaywrightComputerBackend` from `tests.ai_agents.computer_use_backend`
- Import `Persona` from `tests.persona_agents.personas`
- Import `calculate_friction_score` from `tests.persona_agents.friction_scorer`
- Import `anthropic`
- Define module-level constant `PERSONA_OUTPUT_INSTRUCTIONS` — a string instructing Claude to output a JSON block delimited by triple-backtick json ... triple-backtick at the end of its session with fields: task_completed (bool), steps_taken (int), confusion_points (list of {step: int, description: str, severity: "low"|"medium"|"high"}), suggestions (list of str). Include: "IMPORTANT: Your final message MUST include the ```json block before stopping." per RESEARCH Pitfall 2.
- Define `run_persona_scenario(backend: PlaywrightComputerBackend, client: anthropic.Anthropic, persona: Persona, flow_goal: str, optimal_steps: int, max_iterations: int = 25) -> dict[str, Any]`:
  - Build system_prompt by concatenating persona.system_prompt + "\n\nBehavioral constraints:\n" + bulleted constraints + "\n\n" + PERSONA_OUTPUT_INSTRUCTIONS
  - Call `run_agent_scenario(backend=backend, client=client, goal=flow_goal, system_prompt=system_prompt, max_iterations=max_iterations)`
  - Parse structured JSON from raw_result["reasoning"] using `_parse_structured_output()`
  - Calculate friction_score via `calculate_friction_score()`
  - Return dict with keys: persona (str), literacy_level (int), verdict (str), task_completed (bool), friction_score (int), steps_taken (int), optimal_steps (int), confusion_points (list), suggestions (list), tokens_used (int), reasoning (str)
- Define `_parse_structured_output(text: str) -> dict`: regex search for ```json\s*(\{.*?\})\s*``` with re.DOTALL, json.loads the match, return {} on failure

Create `tests/persona_agents/artifact_writer.py` per RESEARCH Pattern 4:
- Function `save_persona_result(result: dict, run_id: str) -> str`:
  - Create directory `reports/persona/{run_id}/` via os.makedirs(exist_ok=True)
  - Filename: `{result['persona']}_{result['flow']}.json`
  - Write result dict as JSON with indent=2
  - Return the file path
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python -c "
from tests.persona_agents.persona_runner import run_persona_scenario, _parse_structured_output, PERSONA_OUTPUT_INSTRUCTIONS
assert 'task_completed' in PERSONA_OUTPUT_INSTRUCTIONS
assert 'confusion_points' in PERSONA_OUTPUT_INSTRUCTIONS
parsed = _parse_structured_output('some text\n\`\`\`json\n{\"task_completed\": true, \"steps_taken\": 3, \"confusion_points\": [], \"suggestions\": [\"add labels\"]}\n\`\`\`\nmore text')
assert parsed['task_completed'] is True
assert parsed['steps_taken'] == 3
empty = _parse_structured_output('no json here')
assert empty == {}
from tests.persona_agents.artifact_writer import save_persona_result
print('ALL IMPORTS AND PARSING PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - tests/persona_agents/persona_runner.py contains `from tests.ai_agents.agent_runner import run_agent_scenario`
    - tests/persona_agents/persona_runner.py contains `def run_persona_scenario(`
    - tests/persona_agents/persona_runner.py contains `def _parse_structured_output(`
    - tests/persona_agents/persona_runner.py contains `PERSONA_OUTPUT_INSTRUCTIONS`
    - _parse_structured_output correctly extracts JSON from triple-backtick blocks
    - _parse_structured_output returns {} when no JSON block found
    - tests/persona_agents/artifact_writer.py contains `def save_persona_result(`
    - artifact_writer creates directories via os.makedirs with exist_ok=True
  </acceptance_criteria>
  <done>Persona runner wraps agent_runner with persona prompts and structured output parsing; artifact writer saves JSON files to reports/persona/</done>
</task>

</tasks>

<verification>
- `python -c "from tests.persona_agents.personas import ALL_PERSONAS; assert len(ALL_PERSONAS) == 5"`
- `python -c "from tests.persona_agents.friction_scorer import calculate_friction_score; assert calculate_friction_score(6, 6, [], True) == 1"`
- `python -c "from tests.persona_agents.persona_runner import run_persona_scenario"`
- `python -c "from tests.persona_agents.artifact_writer import save_persona_result"`
</verification>

<success_criteria>
All 5 persona modules importable without error. Friction scorer returns correct boundary values (1 for optimal, 10 for failure). JSON parser extracts structured output from Claude response text. Artifact writer creates directory structure and writes JSON files.
</success_criteria>

<output>
After completion, create `.planning/phases/07-digital-literacy-persona-agents/07-01-SUMMARY.md`
</output>
