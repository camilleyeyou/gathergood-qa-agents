---
phase: 03-checkout-orders-check-in
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/api/test_checkin.py
autonomous: true
requirements:
  - TCHKN-01
  - TCHKN-02
  - TCHKN-03
  - TCHKN-04
  - TCHKN-05
  - TCHKN-06
  - TMCHK-01
  - TMCHK-02
  - TSTAT-01
  - TSTAT-02
  - TSRCH-01

must_haves:
  truths:
    - "QR scan of valid ticket returns status success with attendee_name, attendee_email, tier_name, checked_in_at"
    - "Re-scan of already checked-in ticket returns status already_checked_in with original checked_in_at"
    - "Scan with forged HMAC returns status invalid with signature verification message"
    - "TCHKN-05 is skipped with reason mentioning no ticket cancel API"
    - "Manual check-in by ticket ID returns status success with attendee_name, tier_name"
    - "Manual check-in response has same status/message keys as QR scan (but not attendee_email/checked_in_at)"
    - "Stats endpoint returns total_registered, checked_in, not_checked_in, percentage, by_tier"
    - "by_tier is array of objects with tier_name, total, checked_in"
    - "Search by name, email, or confirmation code each return matching attendees"
  artifacts:
    - path: "tests/api/test_checkin.py"
      provides: "11 check-in tests covering TCHKN-01 through TCHKN-06, TMCHK-01, TMCHK-02, TSTAT-01, TSTAT-02, TSRCH-01"
      min_lines: 150
  key_links:
    - from: "tests/api/test_checkin.py"
      to: "POST /organizations/{slug}/events/{slug}/check-in/scan/"
      via: "auth_client.post"
      pattern: "check-in/scan/"
    - from: "tests/api/test_checkin.py"
      to: "POST /organizations/{slug}/events/{slug}/check-in/{ticket_id}/manual/"
      via: "auth_client.post"
      pattern: "check-in.*manual/"
    - from: "tests/api/test_checkin.py"
      to: "GET /organizations/{slug}/events/{slug}/check-in/stats/"
      via: "auth_client.get"
      pattern: "check-in/stats/"
    - from: "tests/api/test_checkin.py"
      to: "GET /organizations/{slug}/events/{slug}/check-in/search/"
      via: "auth_client.get"
      pattern: "check-in/search/"
---

<objective>
Write all 11 check-in API tests covering QR scan, manual check-in, stats, and attendee search (TCHKN-01 through TCHKN-06, TMCHK-01, TMCHK-02, TSTAT-01, TSTAT-02, TSRCH-01).

Purpose: Verify the full check-in lifecycle — successful QR scan, re-scan detection, forged QR rejection, HMAC enforcement, manual check-in, stats breakdown, and attendee search across name/email/code fields.

Output: tests/api/test_checkin.py with 11 tests (1 skipped for TCHKN-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-checkout-orders-check-in/03-RESEARCH.md
@.planning/phases/03-checkout-orders-check-in/03-01-SUMMARY.md

@conftest.py
@tests/api/conftest.py
@factories/common.py
@helpers/api.py

<interfaces>
<!-- From RESEARCH.md — verified API contracts -->

POST /organizations/{org_slug}/events/{event_slug}/check-in/scan/
  Body: {"qr_data": "order_uuid:tier_uuid:ticket_uuid:hmac_16hex"}
  Success (200): {"status": "success", "message": "Checked in successfully!", "attendee_name": "...", "attendee_email": "...", "tier_name": "...", "checked_in_at": "..."}
  Already (200): {"status": "already_checked_in", "message": "Already checked in at HH:MM PM.", "attendee_name": "...", "tier_name": "...", "checked_in_at": "..."}
  Invalid (200): {"status": "invalid", "message": "Invalid QR code. Signature verification failed."}

POST /organizations/{org_slug}/events/{event_slug}/check-in/{ticket_id}/manual/
  No body needed
  Success (200): {"status": "success", "message": "Checked in successfully!", "attendee_name": "...", "tier_name": "..."}
  NOTE: No attendee_email or checked_in_at in manual success response

GET /organizations/{org_slug}/events/{event_slug}/check-in/stats/
  200: {"total_registered": N, "checked_in": N, "not_checked_in": N, "percentage": N.N, "by_tier": [{"tier_name": "...", "total": N, "checked_in": N}]}

GET /organizations/{org_slug}/events/{event_slug}/check-in/search/?q={term}
  200: [{"ticket_id": "uuid", "attendee_name": "...", "attendee_email": "...", "tier_name": "...", "confirmation_code": "...", "checked_in": bool, "checked_in_at": "..."}]

From conftest.py checkout_event fixture (created by Plan 01):
  Returns {"event": {..., "slug": "..."}, "free_tier": {..., "id": "..."}, "paid_tier": {...}, "promo": {...}}
  org fixture: {"slug": "..."}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write QR scan and HMAC check-in tests (TCHKN-01 through TCHKN-06)</name>
  <files>tests/api/test_checkin.py</files>
  <read_first>
    - .planning/phases/03-checkout-orders-check-in/03-RESEARCH.md (QR Check-In section, Pitfall 6, Pitfall 8, Pitfall 10, QR Code Format section)
    - tests/api/conftest.py (checkout_event fixture)
    - .planning/phases/03-checkout-orders-check-in/03-01-SUMMARY.md
  </read_first>
  <action>
Create tests/api/test_checkin.py with module docstring "Check-in API tests — TCHKN-01 through TCHKN-06, TMCHK-01, TMCHK-02, TSTAT-01, TSTAT-02, TSRCH-01."

Import pytest, assert_status from helpers.api, unique_email from factories.common.

Define a module-level helper `_checkin_base(org, checkout_event)` that returns f"/organizations/{org['slug']}/events/{checkout_event['event']['slug']}/check-in"

Define a module-scoped fixture `checkin_order` that:
- Takes auth_client, org, checkout_event
- POSTs /checkout/ with action="complete", org_slug=org["slug"], event_slug=checkout_event["event"]["slug"], items=[{"tier_id": checkout_event["free_tier"]["id"], "quantity": 2}], promo_code="", billing_name="ScanTest Attendee", billing_email=unique_email(), billing_phone=""
- assert_status(resp, 201)
- Returns resp.json() — this order has 2 tickets for scan/manual testing

Define a module-scoped fixture `checkin_base_url(org, checkout_event)` that returns the base URL string.

**TCHKN-01** `test_scan_qr_success(auth_client, checkin_order, checkin_base_url)`:
- Use first ticket's qr_code_data: qr = checkin_order["tickets"][0]["qr_code_data"]
- POST {checkin_base_url}/scan/ with {"qr_data": qr}
- assert_status(resp, 200)
- data = resp.json(); assert data["status"] == "success"

**TCHKN-02** `test_scan_success_response_shape(auth_client, checkin_order, checkin_base_url)`:
- This test runs AFTER TCHKN-01 scanned ticket[0]. Use the SAME ticket — it will be already_checked_in.
- Actually, better approach: use the SECOND ticket (tickets[1]) for a fresh scan in this test.
- qr = checkin_order["tickets"][1]["qr_code_data"]
- POST {checkin_base_url}/scan/ with {"qr_data": qr}
- assert_status(resp, 200)
- data = resp.json()
- assert data["status"] == "success"
- assert "attendee_name" in data
- assert "attendee_email" in data
- assert "tier_name" in data
- assert "checked_in_at" in data
- assert data["message"] == "Checked in successfully!"

**TCHKN-03** `test_rescan_returns_already_checked_in(auth_client, checkin_order, checkin_base_url)`:
- Re-scan the SAME ticket from TCHKN-02: qr = checkin_order["tickets"][1]["qr_code_data"]
- POST {checkin_base_url}/scan/ with {"qr_data": qr}
- assert_status(resp, 200)
- data = resp.json(); assert data["status"] == "already_checked_in"
- assert "Already checked in" in data["message"]
- assert "checked_in_at" in data

**TCHKN-04** `test_scan_invalid_qr_returns_invalid(auth_client, checkin_base_url)`:
- POST {checkin_base_url}/scan/ with {"qr_data": "not-a-valid-qr-string"}
- assert_status(resp, 200)
- data = resp.json(); assert data["status"] == "invalid"
- assert "Invalid QR code" in data.get("message", "") or "Signature verification failed" in data.get("message", "")

**TCHKN-05** `test_cancelled_ticket_scan_returns_invalid()`:
- Decorated with @pytest.mark.skip(reason="Cannot test: no API endpoint exists to cancel individual tickets. Event cancellation does not invalidate tickets for check-in (empirically verified). This is a backend limitation.")
- pass

**TCHKN-06** `test_hmac_verification_on_scan(auth_client, checkin_order, checkin_base_url)`:
- Take a valid QR and forge the HMAC: qr = checkin_order["tickets"][0]["qr_code_data"]
- parts = qr.rsplit(":", 1) — split on last colon to get [uuid_part, hmac]
- Actually, since UUIDs have dashes not colons, qr.split(":") gives 4 parts. Replace last part with "aaaaaaaaaaaaaaaa" (16 a's)
- forged_parts = qr.split(":"); forged_parts[3] = "aaaaaaaaaaaaaaaa"; forged = ":".join(forged_parts)
- POST {checkin_base_url}/scan/ with {"qr_data": forged}
- assert_status(resp, 200)
- data = resp.json(); assert data["status"] == "invalid"
- assert "Signature verification failed" in data.get("message", "")

IMPORTANT: Tests TCHKN-01 and TCHKN-02 each scan a DIFFERENT ticket from the 2-ticket order. TCHKN-03 re-scans the same ticket as TCHKN-02 to get already_checked_in. This ordering is critical — if tests run in file order (pytest default), this works correctly.

However, to avoid ordering dependency, consider using a dedicated fixture for the "scan-once" ticket. Actually, pytest runs tests in file order by default and module-scoped fixtures persist, so the ordering is deterministic. Add a comment noting this.
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python -c "
import ast, sys
tree = ast.parse(open('tests/api/test_checkin.py').read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
print(f'Test functions: {len(funcs)}')
for f in funcs: print(f'  {f}')
req_ids = ['TCHKN-01','TCHKN-02','TCHKN-03','TCHKN-04','TCHKN-05','TCHKN-06']
src = open('tests/api/test_checkin.py').read()
for rid in req_ids:
    assert rid in src, f'Missing {rid}'
print('All TCHKN req IDs present')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TCHKN-01" tests/api/test_checkin.py
    - grep -q "TCHKN-06" tests/api/test_checkin.py
    - grep -q "check-in/scan/" tests/api/test_checkin.py
    - grep -q "aaaaaaaaaaaaaaaa" tests/api/test_checkin.py (forged HMAC)
    - grep -q "pytest.mark.skip" tests/api/test_checkin.py (TCHKN-05 skipped)
    - grep -q "already_checked_in" tests/api/test_checkin.py
    - grep -q "Signature verification failed" tests/api/test_checkin.py
  </acceptance_criteria>
  <done>6 check-in tests exist (5 active + 1 skipped). QR scan success, re-scan detection, invalid QR, and HMAC forgery rejection all verified.</done>
</task>

<task type="auto">
  <name>Task 2: Write manual check-in, stats, and search tests (TMCHK-01, TMCHK-02, TSTAT-01, TSTAT-02, TSRCH-01)</name>
  <files>tests/api/test_checkin.py</files>
  <read_first>
    - tests/api/test_checkin.py (after Task 1)
    - .planning/phases/03-checkout-orders-check-in/03-RESEARCH.md (Manual check-in, Stats, Search sections, Pitfall 6)
  </read_first>
  <action>
Add 5 more tests to the EXISTING tests/api/test_checkin.py file (append to end, do NOT overwrite Task 1's tests).

Define a module-scoped fixture `manual_checkin_order` that:
- Takes auth_client, org, checkout_event
- Creates a SEPARATE order (not checkin_order) with 1 free ticket for manual check-in testing
- billing_name="ManualTest User", billing_email=unique_email()
- Returns resp.json()

**TMCHK-01** `test_manual_checkin_by_ticket_id(auth_client, org, checkout_event, manual_checkin_order, checkin_base_url)`:
- ticket_id = manual_checkin_order["tickets"][0]["id"]
- POST {checkin_base_url}/{ticket_id}/manual/
- assert_status(resp, 200)
- data = resp.json(); assert data["status"] == "success"
- assert data["message"] == "Checked in successfully!"
- assert "attendee_name" in data

**TMCHK-02** `test_manual_checkin_response_format(auth_client, org, checkout_event, manual_checkin_order, checkin_base_url)`:
- Re-do manual check-in on same ticket — should get already_checked_in OR test the response shape from TMCHK-01
- Better approach: the manual_checkin_order fixture is module-scoped, so after TMCHK-01 the ticket is checked in. Do a second manual check-in on same ticket to verify response shape.
- ticket_id = manual_checkin_order["tickets"][0]["id"]
- POST {checkin_base_url}/{ticket_id}/manual/
- assert_status(resp, 200)
- data = resp.json()
- Common fields with QR: assert "status" in data; assert "message" in data
- Manual success does NOT include attendee_email or checked_in_at (Pitfall 6)
- Since ticket already checked in, data["status"] will be "already_checked_in"
- assert data["status"] == "already_checked_in"
- NOTE: The already_checked_in manual response does NOT include attendee_name or tier_name either (per RESEARCH.md). Assert that status and message are present (the common fields).

**TSTAT-01** `test_checkin_stats_fields(auth_client, checkin_base_url)`:
- GET {checkin_base_url}/stats/
- assert_status(resp, 200)
- data = resp.json()
- assert "total_registered" in data; assert isinstance(data["total_registered"], int)
- assert "checked_in" in data; assert isinstance(data["checked_in"], int)
- assert "not_checked_in" in data; assert isinstance(data["not_checked_in"], int)
- assert "percentage" in data; assert isinstance(data["percentage"], (int, float))
- assert "by_tier" in data
- assert data["total_registered"] == data["checked_in"] + data["not_checked_in"]

**TSTAT-02** `test_checkin_stats_by_tier_breakdown(auth_client, checkin_base_url)`:
- GET {checkin_base_url}/stats/
- assert_status(resp, 200)
- data = resp.json(); tiers = data["by_tier"]
- assert isinstance(tiers, list); assert len(tiers) >= 1
- for tier in tiers: assert "tier_name" in tier; assert "total" in tier; assert "checked_in" in tier

**TSRCH-01** `test_search_attendees(auth_client, checkin_order, checkin_base_url)`:
- Three sub-tests (search by name, email, confirmation code):
  a) GET {checkin_base_url}/search/?q=ScanTest -> assert_status(200); results = resp.json(); assert len(results) >= 1; assert any("ScanTest" in r.get("attendee_name", "") for r in results)
  b) GET {checkin_base_url}/search/?q={checkin_order["billing_email"]} -> results should match (at least the domain or partial email)
     Actually use the attendee_email from the order: search by the unique email's domain segment which is ".invalid"
     Better: search by the confirmation_code: GET {checkin_base_url}/search/?q={checkin_order["confirmation_code"]} -> assert len >= 1
  c) Verify search result shape: result has ticket_id, attendee_name, attendee_email, tier_name, confirmation_code, checked_in

Each test decorated with @pytest.mark.req("TMCHK-NN" or "TSTAT-NN" or "TSRCH-01").
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && pytest tests/api/test_checkin.py -x --tb=short -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TMCHK-01" tests/api/test_checkin.py
    - grep -q "TMCHK-02" tests/api/test_checkin.py
    - grep -q "TSTAT-01" tests/api/test_checkin.py
    - grep -q "TSTAT-02" tests/api/test_checkin.py
    - grep -q "TSRCH-01" tests/api/test_checkin.py
    - grep -q "manual/" tests/api/test_checkin.py
    - grep -q "check-in/stats/" tests/api/test_checkin.py
    - grep -q "check-in/search/" tests/api/test_checkin.py
    - grep -q "by_tier" tests/api/test_checkin.py
    - pytest tests/api/test_checkin.py runs with 10 passed, 1 skipped
  </acceptance_criteria>
  <done>All 11 check-in tests exist (10 active + 1 skipped TCHKN-05). QR scan, re-scan, invalid QR, HMAC forge, manual check-in, stats fields, per-tier breakdown, and attendee search all verified against the live API.</done>
</task>

</tasks>

<verification>
pytest tests/api/test_checkin.py -v --tb=short 2>&1 | grep -E "PASSED|FAILED|SKIPPED|ERROR"
# Expected: 10 PASSED, 1 SKIPPED (TCHKN-05)
</verification>

<success_criteria>
- 11 test functions exist in test_checkin.py, each with @pytest.mark.req
- QR scan tests use POST /organizations/{slug}/events/{slug}/check-in/scan/ with {"qr_data": "..."}
- Manual check-in tests use POST /organizations/{slug}/events/{slug}/check-in/{ticket_id}/manual/
- Stats tests verify total_registered, checked_in, not_checked_in, percentage, by_tier
- Search test verifies search by name and confirmation code
- TCHKN-05 is cleanly skipped with backend limitation reason
- 10 tests pass, 1 skipped
</success_criteria>

<output>
After completion, create `.planning/phases/03-checkout-orders-check-in/03-03-SUMMARY.md`
</output>
