---
phase: 02-core-api-tests
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - tests/api/test_events.py
  - tests/api/test_ticket_tiers.py
  - tests/api/test_promos.py
autonomous: true
requirements:
  - TEVNT-01
  - TEVNT-02
  - TEVNT-03
  - TEVNT-04
  - TEVNT-05
  - TEVNT-06
  - TEVNT-07
  - TTICK-01
  - TTICK-02
  - TTICK-03
  - TTICK-04
  - TPRMO-01
  - TPRMO-02
  - TPRMO-03
  - TPRMO-04
must_haves:
  truths:
    - "Event creation with all fields returns 201 with DRAFT status"
    - "Event slug is auto-generated from title with dedup suffix"
    - "Publishing a DRAFT event changes status to PUBLISHED"
    - "Cancelling an event from any status changes status to CANCELLED"
    - "Cannot publish a CANCELLED event (400)"
    - "Cannot publish an already-PUBLISHED event (400)"
    - "Ticket tier creation returns quantity_remaining = quantity_total"
    - "All three visibility options (PUBLIC, HIDDEN, INVITE_ONLY) are accepted"
    - "Soft-deleting a tier sets is_active to false"
    - "Promo code is stored uppercase regardless of input case"
    - "Empty applicable_tier_ids means code applies to all tiers"
    - "Promo validate endpoint requires auth and checks active/expired/tier"
  artifacts:
    - path: "tests/api/test_events.py"
      provides: "7 event tests mapped to TEVNT-01 through TEVNT-07"
      contains: "@pytest.mark.req"
    - path: "tests/api/test_ticket_tiers.py"
      provides: "4 ticket tier tests mapped to TTICK-01 through TTICK-04"
      contains: "@pytest.mark.req"
    - path: "tests/api/test_promos.py"
      provides: "4 promo code tests mapped to TPRMO-01 through TPRMO-04"
      contains: "@pytest.mark.req"
  key_links:
    - from: "tests/api/test_events.py"
      to: "/organizations/{slug}/events/"
      via: "httpx POST/GET/PATCH plus /publish/ and /cancel/ actions"
      pattern: "/events/"
    - from: "tests/api/test_ticket_tiers.py"
      to: "/organizations/{slug}/events/{event-slug}/ticket-tiers/"
      via: "httpx POST/PATCH/DELETE"
      pattern: "/ticket-tiers/"
    - from: "tests/api/test_promos.py"
      to: "/organizations/{slug}/events/{event-slug}/promo-codes/"
      via: "httpx POST/PATCH plus /validate/"
      pattern: "/promo-codes/"
---

<objective>
Write event, ticket tier, and promo code API tests covering the full event lifecycle, tier management, and promo validation.

Purpose: Events, tiers, and promos are the deepest resources in the dependency chain. They depend on org and venue fixtures from Plan 01/03. This plan covers the remaining 15 requirements.

Output: test_events.py (7 tests), test_ticket_tiers.py (4 tests), test_promos.py (4 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-api-tests/02-RESEARCH.md
@conftest.py
@tests/api/conftest.py
@factories/common.py
@helpers/api.py

<interfaces>
From tests/api/conftest.py (Plan 01):
```python
@pytest.fixture(scope="session")
def org(auth_client, teardown_registry) -> dict:
    # {"id": uuid, "slug": str, "name": str, "role": "OWNER", ...}

@pytest.fixture(scope="session")
def venue(auth_client, org, teardown_registry) -> dict:
    # {"id": uuid, "name": str, "address": str, "capacity": int, ...}

@pytest.fixture(scope="session")
def event(auth_client, org, teardown_registry) -> dict:
    # {"id": uuid, "slug": str, "title": str, "status": "DRAFT", ...}

@pytest.fixture(scope="session")
def published_event(auth_client, org, teardown_registry) -> dict:
    # {"id": uuid, "slug": str, "status": "PUBLISHED", ...}
```

From conftest.py:
```python
# auth_client: .get(), .post(), .patch(), .delete() -- Bearer token auto-refresh
# teardown_registry: dict with event_ids, ticket_tier_ids, promo_code_ids lists
```

From factories/common.py:
```python
def event_title() -> str   # test-{run}-event-{token}
def tier_name() -> str      # test-{run}-tier-{token}
def promo_code() -> str     # TEST{RUN}{TOKEN} (uppercase)
```

API field names (CRITICAL from RESEARCH.md):
- Event creation: start_datetime, end_datetime (NOT start_date/start_time)
- Event creation: venue (UUID string, NOT venue_id)
- Event format: IN_PERSON | VIRTUAL | HYBRID
- Event category: FUNDRAISER | WORKSHOP | MEETUP | VOLUNTEER | SOCIAL | OTHER
- Ticket visibility: PUBLIC | HIDDEN | INVITE_ONLY
- Promo discount_type: PERCENTAGE | FIXED
- All org/event URLs use SLUGS, never UUIDs
- Venue/tier/promo detail URLs use UUIDs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write event lifecycle tests (TEVNT-01 through TEVNT-07)</name>
  <files>tests/api/test_events.py</files>
  <read_first>tests/api/conftest.py, conftest.py, factories/common.py, helpers/api.py, .planning/phases/02-core-api-tests/02-RESEARCH.md</read_first>
  <action>
Create `tests/api/test_events.py` with 7 tests. Event status transitions need separate events to avoid ordering dependencies.

**TEVNT-01: test_create_event_all_fields(auth_client, org, venue, teardown_registry)**
- POST `/organizations/{org['slug']}/events/` with ALL fields:
  ```json
  {
    "title": event_title(),
    "description": "Full field test event",
    "format": "IN_PERSON",
    "category": "FUNDRAISER",
    "start_datetime": "2026-12-15T09:00:00",
    "end_datetime": "2026-12-15T17:00:00",
    "timezone": "America/New_York",
    "venue": venue["id"],
    "tags": ["test", "phase2"]
  }
  ```
- CRITICAL: field is `venue` NOT `venue_id`. Field is `start_datetime` NOT `start_date`.
- assert_status(resp, 201, "POST create event")
- Assert response has: `title`, `slug`, `status`, `format`, `category`, `start_datetime`, `end_datetime`
- Assert `data["format"] == "IN_PERSON"` and `data["category"] == "FUNDRAISER"`
- Register: `teardown_registry["event_ids"].append({"org_slug": org["slug"], "event_slug": data["slug"]})`

**TEVNT-02: test_event_defaults_to_draft(auth_client, org, teardown_registry)**
- Create a minimal event (title + format + category + start/end datetime + timezone)
- assert_status 201
- Assert `data["status"] == "DRAFT"`
- Register in teardown

**TEVNT-03: test_event_slug_auto_generated(auth_client, org, teardown_registry)**
- Create event with known title like `event_title()` (e.g., "test-abc123-event-def4")
- Assert `"slug" in data`
- Assert slug is derived from title (contains lowercase version of title keywords)
- Create a SECOND event with SAME title to verify dedup:
  Second slug should differ from first (append "-1" or similar)
- Register both in teardown

**TEVNT-04: test_publish_draft_event(auth_client, org, teardown_registry)**
- Create a NEW draft event
- POST `/organizations/{org['slug']}/events/{event_slug}/publish/`
- assert_status(resp, 200, "POST publish event")
- Assert `data["status"] == "PUBLISHED"`
- Register in teardown

**TEVNT-05: test_cancel_event_from_any_status(auth_client, org, teardown_registry)**
- Create a draft event, cancel it:
  POST `/organizations/{org['slug']}/events/{slug}/cancel/`
  assert_status 200, assert `data["status"] == "CANCELLED"`
- Create another event, PUBLISH it first, then cancel it:
  POST publish, then POST cancel
  assert_status 200, assert `data["status"] == "CANCELLED"`
- Register both in teardown

**TEVNT-06: test_cannot_publish_cancelled_event(auth_client, org, teardown_registry)**
- Create event, cancel it, then try to publish:
  POST `/organizations/{org['slug']}/events/{slug}/publish/`
- assert_status(resp, 400)
- Assert `"Only draft events can be published"` in response text
- Register in teardown

**TEVNT-07: test_cannot_publish_already_published_event(auth_client, org, teardown_registry)**
- Create event, publish it, then try to publish again:
  POST `/organizations/{org['slug']}/events/{slug}/publish/`
- assert_status(resp, 400)
- Assert `"Only draft events can be published"` in response text
- Register in teardown

Each status-transition test creates its OWN event to avoid ordering dependencies between tests. Do NOT reuse the session `event` fixture for mutation tests.

Imports: pytest, factories.common.event_title, helpers.api.assert_status
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python3 -m pytest tests/api/test_events.py -v --tb=short -x --no-header -p no:html 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_events.py contains exactly 7 test functions
    - grep '@pytest.mark.req("TEVNT-01")' tests/api/test_events.py returns match
    - grep '@pytest.mark.req("TEVNT-02")' tests/api/test_events.py returns match
    - grep '@pytest.mark.req("TEVNT-03")' tests/api/test_events.py returns match
    - grep '@pytest.mark.req("TEVNT-04")' tests/api/test_events.py returns match
    - grep '@pytest.mark.req("TEVNT-05")' tests/api/test_events.py returns match
    - grep '@pytest.mark.req("TEVNT-06")' tests/api/test_events.py returns match
    - grep '@pytest.mark.req("TEVNT-07")' tests/api/test_events.py returns match
    - grep 'start_datetime' tests/api/test_events.py returns matches (correct field name)
    - grep 'start_date[^t]' tests/api/test_events.py returns NO matches (wrong field name)
    - grep '"venue"' tests/api/test_events.py returns match (correct field, not venue_id)
    - grep 'Only draft events can be published' tests/api/test_events.py returns matches
    - All 7 tests pass against live API
  </acceptance_criteria>
  <done>7 event lifecycle tests pass. DRAFT/PUBLISHED/CANCELLED transitions verified. All field names match live API (start_datetime, venue).</done>
</task>

<task type="auto">
  <name>Task 2: Write ticket tier tests (TTICK-01 through TTICK-04)</name>
  <files>tests/api/test_ticket_tiers.py</files>
  <read_first>tests/api/conftest.py, conftest.py, factories/common.py, helpers/api.py, .planning/phases/02-core-api-tests/02-RESEARCH.md</read_first>
  <action>
Create `tests/api/test_ticket_tiers.py` with 4 tests. Tiers are nested under org slug + event slug.

Base URL pattern: `/organizations/{org['slug']}/events/{event['slug']}/ticket-tiers/`

Use the session `event` fixture (DRAFT event) for tier creation. Tiers are created under events.

**TTICK-01: test_create_tier_all_options(auth_client, org, event, teardown_registry)**
- POST `/organizations/{org['slug']}/events/{event['slug']}/ticket-tiers/` with:
  ```json
  {
    "name": tier_name(),
    "price": "25.00",
    "quantity_total": 100,
    "min_per_order": 1,
    "max_per_order": 5,
    "visibility": "PUBLIC"
  }
  ```
- assert_status(resp, 201, "POST create tier")
- Assert response has: `name`, `price`, `quantity_total`, `quantity_remaining`, `quantity_sold`, `min_per_order`, `max_per_order`, `visibility`, `is_active`, `id`
- Assert `data["visibility"] == "PUBLIC"` and `data["is_active"] is True`
- Register: `teardown_registry["ticket_tier_ids"].append({"org_slug": org["slug"], "event_slug": event["slug"], "tier_id": data["id"]})`

**TTICK-02: test_quantity_remaining_calculated(auth_client, org, event, teardown_registry)**
- Create tier with `quantity_total: 50`
- assert_status 201
- Assert `data["quantity_total"] == 50`
- Assert `data["quantity_sold"] == 0`
- Assert `data["quantity_remaining"] == 50` (computed: total - sold)
- Register in teardown

**TTICK-03: test_visibility_options(auth_client, org, event, teardown_registry)**
- Create 3 tiers, one with each visibility: PUBLIC, HIDDEN, INVITE_ONLY
- For each: assert_status 201, assert `data["visibility"]` matches what was sent
- Register all 3 in teardown

**TTICK-04: test_soft_delete_tier(auth_client, org, event, teardown_registry)**
- Create a tier
- PATCH `/organizations/{org['slug']}/events/{event['slug']}/ticket-tiers/{tier_id}/` with `{"is_active": false}`
- assert_status(resp, 200, "PATCH soft-delete tier")
- Assert `data["is_active"] is False`
- Note: tier_id is UUID from create response `data["id"]`
- Register in teardown (even though deactivated)

Imports: pytest, factories.common.tier_name, helpers.api.assert_status
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python3 -m pytest tests/api/test_ticket_tiers.py -v --tb=short -x --no-header -p no:html 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_ticket_tiers.py contains exactly 4 test functions
    - grep '@pytest.mark.req("TTICK-01")' tests/api/test_ticket_tiers.py returns match
    - grep '@pytest.mark.req("TTICK-02")' tests/api/test_ticket_tiers.py returns match
    - grep '@pytest.mark.req("TTICK-03")' tests/api/test_ticket_tiers.py returns match
    - grep '@pytest.mark.req("TTICK-04")' tests/api/test_ticket_tiers.py returns match
    - grep 'quantity_remaining' tests/api/test_ticket_tiers.py returns matches
    - grep 'HIDDEN\|INVITE_ONLY' tests/api/test_ticket_tiers.py returns matches (visibility options)
    - grep 'is_active.*false\|is_active.*False' tests/api/test_ticket_tiers.py returns match
    - All tiers registered in teardown with org_slug, event_slug, tier_id
    - All 4 tests pass against live API
  </acceptance_criteria>
  <done>4 ticket tier tests pass. All visibility options verified. quantity_remaining computed correctly. Soft-delete sets is_active=false.</done>
</task>

<task type="auto">
  <name>Task 3: Write promo code tests (TPRMO-01 through TPRMO-04)</name>
  <files>tests/api/test_promos.py</files>
  <read_first>tests/api/conftest.py, conftest.py, factories/common.py, helpers/api.py, .planning/phases/02-core-api-tests/02-RESEARCH.md</read_first>
  <action>
Create `tests/api/test_promos.py` with 4 tests. Promos are nested under org slug + event slug.

Base URL pattern: `/organizations/{org['slug']}/events/{event['slug']}/promo-codes/`

Use the session `event` fixture for promo creation. Need a tier for TPRMO-03 and TPRMO-04 testing -- create one inline or use a fixture.

**TPRMO-01: test_create_promo_percentage_and_fixed(auth_client, org, event, teardown_registry)**
- Create PERCENTAGE promo:
  POST with `{"code": promo_code(), "discount_type": "PERCENTAGE", "discount_value": "10.00", "usage_limit": 100}`
  assert_status 201
  Assert `data["discount_type"] == "PERCENTAGE"` and `data["discount_value"]` is "10.00" (or numeric 10)
- Create FIXED promo:
  POST with `{"code": promo_code(), "discount_type": "FIXED", "discount_value": "5.00", "usage_limit": 50}`
  assert_status 201
  Assert `data["discount_type"] == "FIXED"`
- Register both: `teardown_registry["promo_code_ids"].append({"org_slug": org["slug"], "event_slug": event["slug"], "promo_id": data["id"]})`

**TPRMO-02: test_promo_code_stored_uppercase(auth_client, org, event, teardown_registry)**
- Create promo with lowercase code: `{"code": "lowercasecode", "discount_type": "PERCENTAGE", "discount_value": "10.00"}`
- assert_status 201
- Assert `data["code"] == "LOWERCASECODE"` (auto-uppercased by API)
- Register in teardown

**TPRMO-03: test_empty_tier_ids_applies_to_all(auth_client, org, event, teardown_registry)**
- First create a ticket tier under the event (needed for validate test):
  POST tier with `{"name": tier_name(), "price": "0.00", "quantity_total": 10}`
  Register tier in teardown
- Create promo with `applicable_tier_ids: []` (empty = all tiers):
  POST with `{"code": promo_code(), "discount_type": "PERCENTAGE", "discount_value": "15.00", "applicable_tier_ids": []}`
  assert_status 201
- Validate the promo with a specific tier_id:
  POST `/organizations/{org['slug']}/events/{event['slug']}/promo-codes/validate/`
  with `{"code": data["code"], "tier_id": tier_data["id"]}`
  CRITICAL: This endpoint REQUIRES authentication (use auth_client, not anonymous)
- assert_status(validate_resp, 200)
- Assert `validate_resp.json()["valid"] is True`
- Register promo in teardown

**TPRMO-04: test_validate_promo_active_expired_tier(auth_client, org, event, teardown_registry)**
- Create an active promo with `applicable_tier_ids: []` and validate it:
  POST create, then POST validate with `{"code": code}`
  Assert `valid: True`
- Create a deactivated promo (create, then PATCH `is_active: false`):
  POST validate with the deactivated code
  Assert `valid: False` or appropriate error response
- Create a promo with a specific `applicable_tier_ids: [tier_id]`:
  Create a second tier, create promo tied to that tier only
  Validate with a DIFFERENT tier_id -- should return `valid: False`
- CRITICAL: validate endpoint is authenticated -- always use auth_client
- Register all promos and tiers in teardown

Note on usage_limit testing: Per RESEARCH.md Pitfall 8, usage_limit=0 does not mean "exhausted". Testing "over limit" requires actual checkout (Phase 3). For TPRMO-04, test the cases that can be verified: active vs deactivated, tier scope restriction. Do NOT test usage_limit exhaustion.

Imports: pytest, factories.common.promo_code, tier_name, helpers.api.assert_status
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python3 -m pytest tests/api/test_promos.py -v --tb=short -x --no-header -p no:html 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_promos.py contains exactly 4 test functions
    - grep '@pytest.mark.req("TPRMO-01")' tests/api/test_promos.py returns match
    - grep '@pytest.mark.req("TPRMO-02")' tests/api/test_promos.py returns match
    - grep '@pytest.mark.req("TPRMO-03")' tests/api/test_promos.py returns match
    - grep '@pytest.mark.req("TPRMO-04")' tests/api/test_promos.py returns match
    - grep 'PERCENTAGE\|FIXED' tests/api/test_promos.py returns matches (both discount types)
    - grep 'LOWERCASECODE\|uppercase\|upper' tests/api/test_promos.py returns match
    - grep 'applicable_tier_ids' tests/api/test_promos.py returns matches
    - grep 'validate' tests/api/test_promos.py returns matches (validate endpoint used)
    - grep 'is_active.*false\|is_active.*False' tests/api/test_promos.py returns match (deactivation test)
    - Validate endpoint always called via auth_client (not anonymous)
    - All 4 tests pass against live API
  </acceptance_criteria>
  <done>4 promo tests pass. Both discount types created. Uppercase storage verified. Empty tier_ids = all tiers. Validate checks active status and tier scope.</done>
</task>

</tasks>

<verification>
- `pytest tests/api/test_events.py tests/api/test_ticket_tiers.py tests/api/test_promos.py -v --tb=short -p no:html` -- all 15 tests pass
- Each test has a @pytest.mark.req marker with correct ID
- `pytest tests/api/ -v --tb=short -p no:html` -- full API suite (all 34 tests from all plans) passes
</verification>

<success_criteria>
- 15 tests across 3 files all pass against live backend
- Event field names: start_datetime, end_datetime, venue (not venue_id)
- Event categories: only valid choices used (FUNDRAISER, WORKSHOP, MEETUP, VOLUNTEER, SOCIAL, OTHER)
- Status transitions tested with separate events per test (no ordering dependency)
- Promo validate always authenticated
- All created resources registered in teardown with full parent slug context
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-api-tests/02-05-SUMMARY.md`
</output>
