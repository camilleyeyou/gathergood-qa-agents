---
phase: 02-core-api-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - conftest.py
  - factories/common.py
  - tests/api/conftest.py
autonomous: true
requirements:
  - TORG-01
  - TVENU-01
  - TEVNT-01
  - TEVNT-02
must_haves:
  truths:
    - "Teardown cleanup runs after session and cancels events, deletes tiers/venues, deactivates promos"
    - "Session-scoped org fixture creates a test org and returns slug"
    - "Session-scoped event fixture creates a DRAFT event under the org and returns slug"
    - "Session-scoped venue fixture creates a venue under the org and returns id"
    - "Factory functions for promo codes and tier names exist"
  artifacts:
    - path: "conftest.py"
      provides: "Teardown cleanup logic replacing placeholder comment"
      contains: "ticket_tier_ids"
    - path: "factories/common.py"
      provides: "Additional factory functions for tier_name and promo_code"
      exports: ["tier_name", "promo_code"]
    - path: "tests/api/conftest.py"
      provides: "Session-scoped org, venue, event, published_event fixtures"
      contains: "def org("
  key_links:
    - from: "tests/api/conftest.py"
      to: "conftest.py"
      via: "auth_client and teardown_registry fixture injection"
      pattern: "def org\\(auth_client"
    - from: "conftest.py"
      to: "API endpoints"
      via: "teardown HTTP calls in reverse dependency order"
      pattern: "ticket-tiers.*delete"
---

<objective>
Wire teardown cleanup into conftest.py and create shared session-scoped resource fixtures for all Phase 2 API tests.

Purpose: Every subsequent plan needs an org, venue, and event already created. The teardown must clean up test data after each run. This infrastructure plan enables all test-writing plans to focus purely on assertions.

Output: Updated conftest.py with teardown logic, extended factories, and tests/api/conftest.py with shared fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-api-tests/02-RESEARCH.md
@conftest.py
@factories/common.py
@helpers/api.py
@settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire teardown cleanup and extend factories</name>
  <files>conftest.py, factories/common.py</files>
  <read_first>conftest.py, factories/common.py, .planning/phases/02-core-api-tests/02-RESEARCH.md</read_first>
  <action>
**conftest.py changes:**

Replace the comment `# Cleanup logic wired in Phase 2 once domain endpoints are known` (line 47) with actual teardown logic. The teardown must use the auth_client's internal `_http` client (since auth_client itself is being torn down). Create a dedicated httpx.Client for teardown OR capture tokens before yield.

IMPORTANT: The teardown runs AFTER `yield registry`, so it needs its own HTTP client. Use `httpx.Client(base_url=settings.API_URL, headers={"Authorization": f"Bearer {access_token}"}, timeout=30)` created before yield and used after yield.

The teardown order (reverse dependency) per RESEARCH.md:
1. DELETE ticket tiers: `DELETE /organizations/{org_slug}/events/{event_slug}/ticket-tiers/{tier_id}/` (204)
2. PATCH promo codes: `PATCH /organizations/{org_slug}/events/{event_slug}/promo-codes/{promo_id}/` with `{"is_active": false}` (200)
3. POST cancel events: `POST /organizations/{org_slug}/events/{event_slug}/cancel/` (200)
4. DELETE venues: `DELETE /organizations/{org_slug}/venues/{venue_id}/` (204)
5. Orgs and users cannot be deleted (405) -- skip, naming prefix is isolation

Each teardown step wraps in try/except to avoid masking test failures. Log each teardown action with print() for visibility.

Registry entries must store dicts with parent slugs (not bare IDs):
- `ticket_tier_ids`: `{"org_slug": ..., "event_slug": ..., "tier_id": ...}`
- `promo_code_ids`: `{"org_slug": ..., "event_slug": ..., "promo_id": ...}`
- `event_ids`: `{"org_slug": ..., "event_slug": ...}`
- `venue_ids`: `{"org_slug": ..., "venue_id": ...}`
- `org_ids`: `{"slug": ...}` (informational only, no delete)

The existing registry key structure stays the same (user_ids, org_ids, venue_ids, event_ids, ticket_tier_ids, promo_code_ids, order_ids). The change is that the VALUES are now dicts instead of bare strings.

CRITICAL: The teardown_registry fixture needs access to an authenticated HTTP client. Restructure so the teardown fixture receives the auth credentials. One approach: make teardown_registry depend on a _teardown_client fixture that creates a separate httpx.Client for cleanup.

Actually, simpler approach: move teardown logic into the auth_client fixture's cleanup (after yield), since auth_client already depends on teardown_registry. After `yield instance` and before `instance.close()`, run the teardown using `instance._http` (which still has the Bearer token).

Wait -- the auth_client fixture already depends on teardown_registry. So the teardown_registry yields first and cleans up last. But teardown_registry doesn't have access to auth_client. The cleanest fix: have teardown_registry store a reference to a cleanup client. Set `registry["_cleanup_client"] = None` initially, then in auth_client fixture after creating the _Client, set `teardown_registry["_cleanup_client"] = instance`. In teardown_registry's cleanup (after yield), use `registry["_cleanup_client"]` if set.

Actually simplest: move ALL cleanup into auth_client's teardown (it runs before teardown_registry's teardown since it depends on it in pytest fixture ordering -- wait, that's reversed. pytest tears down in reverse dependency order. auth_client depends on teardown_registry, so auth_client tears down FIRST, then teardown_registry. So put cleanup in auth_client after yield, before close().

Final approach for conftest.py:
```python
@pytest.fixture(scope="session")
def auth_client(teardown_registry):
    # ... existing setup code ...
    instance = _Client()
    yield instance

    # --- Phase 2 teardown cleanup ---
    print("\n[teardown] Cleaning up test resources...")
    _cleanup(instance, teardown_registry)
    instance.close()
```

Add a `_cleanup(client, registry)` function that does the 4-step teardown described above.

Remove `instance.close()` from its current location (after yield) and put it after `_cleanup()`.

**factories/common.py changes:**

Add two new factory functions:
```python
def tier_name() -> str:
    """Return test-{run}-tier-{token}."""
    return f"test-{RUN_ID}-tier-{uuid.uuid4().hex[:4]}"

def promo_code() -> str:
    """Return TEST{RUN}{TOKEN} (will be uppercased by API)."""
    return f"TEST{RUN_ID}{uuid.uuid4().hex[:4]}".upper()
```
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python3 -c "from factories.common import tier_name, promo_code, RUN_ID; print(tier_name()); print(promo_code())" && python3 -c "import conftest; print('teardown wired' if '_cleanup' in dir(conftest) or 'cleanup' in open('conftest.py').read() else 'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - factories/common.py contains `def tier_name()` and `def promo_code()`
    - conftest.py contains `ticket_tier_ids` in teardown logic (not just registry definition)
    - conftest.py contains `promo_code_ids` in teardown logic
    - conftest.py contains `/cancel/` in teardown logic for events
    - conftest.py contains `venue_id` in teardown logic for venue DELETE
    - conftest.py no longer contains the comment "Cleanup logic wired in Phase 2"
    - `python3 -c "from factories.common import tier_name, promo_code"` exits 0
  </acceptance_criteria>
  <done>Teardown cleanup runs all 4 resource types in reverse dependency order. Two new factory functions importable.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared session-scoped API test fixtures</name>
  <files>tests/api/conftest.py</files>
  <read_first>conftest.py, factories/common.py, helpers/api.py, .planning/phases/02-core-api-tests/02-RESEARCH.md</read_first>
  <action>
Create `tests/api/conftest.py` with session-scoped fixtures that create the resource hierarchy needed by all API test modules. These fixtures run once per test session and share the created resources.

**Fixtures to create:**

1. `org(auth_client, teardown_registry)` -- scope=session
   - POST `/organizations/` with `{"name": org_name(), "description": "Phase 2 test org"}`
   - assert_status 201
   - Register in teardown: `teardown_registry["org_ids"].append({"slug": data["slug"]})`
   - Return full response dict (has id, slug, name, role, etc.)

2. `venue(auth_client, org, teardown_registry)` -- scope=session
   - POST `/organizations/{org['slug']}/venues/` with:
     ```json
     {"name": venue_name(), "address": "123 Test St", "city": "Testville", "state": "TS", "postal_code": "12345", "capacity": 500}
     ```
   - assert_status 201
   - Register: `teardown_registry["venue_ids"].append({"org_slug": org["slug"], "venue_id": data["id"]})`
   - Return full response dict

3. `event(auth_client, org, teardown_registry)` -- scope=session
   - POST `/organizations/{org['slug']}/events/` with:
     ```json
     {"title": event_title(), "format": "IN_PERSON", "category": "MEETUP", "start_datetime": "2026-12-01T09:00:00", "end_datetime": "2026-12-01T17:00:00", "timezone": "UTC"}
     ```
   - assert_status 201
   - Register: `teardown_registry["event_ids"].append({"org_slug": org["slug"], "event_slug": data["slug"]})`
   - Return full response dict (status will be "DRAFT")

4. `published_event(auth_client, org, teardown_registry)` -- scope=session
   - Create a SEPARATE event (not the same as `event` fixture)
   - POST create event, then POST `/organizations/{org['slug']}/events/{slug}/publish/`
   - Register in teardown
   - Return the published event dict (status="PUBLISHED")

All fixtures use imports:
```python
from factories.common import org_name, event_title, venue_name
from helpers.api import assert_status
```

CRITICAL: Use slugs in ALL URL paths, never UUIDs for org/event lookups. Use `data["id"]` (UUID) only for venue_id and tier_id in URLs since those endpoints use UUID lookup.
  </action>
  <verify>
    <automated>cd "/Users/user/Test event planning website" && python3 -c "
import ast, sys
with open('tests/api/conftest.py') as f:
    tree = ast.parse(f.read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
required = ['org', 'venue', 'event', 'published_event']
missing = [r for r in required if r not in funcs]
if missing:
    print(f'MISSING fixtures: {missing}')
    sys.exit(1)
print(f'All fixtures present: {required}')
"</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/conftest.py contains `def org(auth_client, teardown_registry)`
    - tests/api/conftest.py contains `def venue(auth_client, org, teardown_registry)`
    - tests/api/conftest.py contains `def event(auth_client, org, teardown_registry)`
    - tests/api/conftest.py contains `def published_event(auth_client, org, teardown_registry)`
    - All fixtures decorated with `@pytest.fixture(scope="session")`
    - All fixtures use `assert_status` for status code checks
    - All fixtures register resources in teardown_registry with dict entries containing parent slugs
    - Event fixture uses `start_datetime` and `end_datetime` (NOT start_date/start_time)
    - No UUID-based org/event URL routing -- all use slug
  </acceptance_criteria>
  <done>Four session-scoped fixtures create org, venue, draft event, and published event. All register for teardown with parent slugs.</done>
</task>

</tasks>

<verification>
- `python3 -c "from factories.common import tier_name, promo_code"` exits 0
- `grep -c "def org\|def venue\|def event\|def published_event" tests/api/conftest.py` returns 4
- `grep "Cleanup logic wired in Phase 2" conftest.py` returns no matches
- `grep "cancel" conftest.py` returns matches in teardown logic
</verification>

<success_criteria>
- Teardown cleanup is wired and handles all 4 resource types
- Session-scoped fixtures for org, venue, event, published_event exist
- All fixtures register for teardown with parent slug context
- Factory functions tier_name() and promo_code() are importable
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-api-tests/02-01-SUMMARY.md`
</output>
