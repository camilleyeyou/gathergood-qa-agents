---
phase: 06-ai-qa-agents
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/ai_agents/scenarios/__init__.py
  - tests/ai_agents/scenarios/test_auth_agent.py
  - tests/ai_agents/scenarios/test_event_management_agent.py
  - tests/ai_agents/scenarios/test_checkout_agent.py
  - tests/ai_agents/scenarios/test_checkin_agent.py
  - tests/ai_agents/scenarios/test_permissions_agent.py
autonomous: true
requirements:
  - AIQA-03
  - AIQA-04
  - AIQA-05
  - AIQA-06
  - AIQA-07
  - AIQA-08

must_haves:
  truths:
    - "Agent tests cover auth flow (register, login, verify dashboard)"
    - "Agent tests cover event management (create org, create event, publish, view public)"
    - "Agent tests cover checkout flow (browse, select tickets, free checkout, confirmation)"
    - "Agent tests cover check-in page (scanner, search, stats UI)"
    - "Agent tests cover permission boundaries (different roles, restricted actions blocked)"
    - "Each scenario has a @pytest.mark.req marker and @pytest.mark.ai_agent marker"
  artifacts:
    - path: "tests/ai_agents/scenarios/test_auth_agent.py"
      provides: "Auth flow agent scenario"
      contains: "def test_auth_flow_agent"
    - path: "tests/ai_agents/scenarios/test_event_management_agent.py"
      provides: "Event management agent scenario"
      contains: "def test_event_management_agent"
    - path: "tests/ai_agents/scenarios/test_checkout_agent.py"
      provides: "Checkout flow agent scenario"
      contains: "def test_checkout_flow_agent"
    - path: "tests/ai_agents/scenarios/test_checkin_agent.py"
      provides: "Check-in page agent scenario"
      contains: "def test_checkin_page_agent"
    - path: "tests/ai_agents/scenarios/test_permissions_agent.py"
      provides: "Permission boundary agent scenario"
      contains: "def test_permission_boundary_agent"
  key_links:
    - from: "tests/ai_agents/scenarios/*.py"
      to: "tests/ai_agents/agent_runner.py"
      via: "run_agent_scenario import"
      pattern: "from tests.ai_agents.agent_runner import run_agent_scenario"
    - from: "tests/ai_agents/scenarios/*.py"
      to: "tests/ai_agents/conftest.py"
      via: "agent_backend and claude_client fixtures"
      pattern: "def test_.*agent_backend.*claude_client"
---

<objective>
Create all AI agent test scenarios covering auth, event management, checkout, check-in, and permission boundaries.

Purpose: Each scenario drives Claude Computer Use through a complete user flow on the live GatherGood site, producing PASS/FAIL/INCONCLUSIVE verdicts with natural language reasoning.
Output: 5 test files in tests/ai_agents/scenarios/ with 8+ test functions total, all marked with @pytest.mark.req and @pytest.mark.ai_agent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-qa-agents/06-RESEARCH.md
@.planning/phases/06-ai-qa-agents/06-01-SUMMARY.md

@tests/ai_agents/agent_runner.py
@tests/ai_agents/conftest.py
@tests/ai_agents/computer_use_backend.py
@tests/ui/conftest.py
@settings.py

<interfaces>
From tests/ai_agents/agent_runner.py:
```python
def run_agent_scenario(
    backend: PlaywrightComputerBackend,
    client: anthropic.Anthropic,
    goal: str,
    system_prompt: str | None = None,
    max_iterations: int = MAX_ITERATIONS,
    max_input_tokens: int = MAX_INPUT_TOKENS,
) -> dict:
    # Returns: {"verdict": str, "reasoning": str, "steps": int, "input_tokens": int, "output_tokens": int}
```

From tests/ai_agents/conftest.py:
```python
# Fixtures available:
# - claude_client (session scope) -> anthropic.Anthropic
# - agent_backend (function scope) -> PlaywrightComputerBackend
# - base_url (session scope) -> str
# - api_url (session scope) -> str
# - agent_system_prompt (session scope) -> str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth and event management agent scenarios</name>
  <files>tests/ai_agents/scenarios/__init__.py, tests/ai_agents/scenarios/test_auth_agent.py, tests/ai_agents/scenarios/test_event_management_agent.py</files>
  <read_first>
    - /Users/user/Test event planning website/tests/ai_agents/agent_runner.py
    - /Users/user/Test event planning website/tests/ai_agents/conftest.py
    - /Users/user/Test event planning website/tests/ui/conftest.py
    - /Users/user/Test event planning website/.planning/phases/06-ai-qa-agents/06-RESEARCH.md
  </read_first>
  <action>
1. Create `tests/ai_agents/scenarios/__init__.py` (empty file).

2. Create `tests/ai_agents/scenarios/test_auth_agent.py` with:
   - Imports: `pytest`, `from tests.ai_agents.agent_runner import run_agent_scenario`
   - Each test function signature: `def test_xxx(agent_backend, claude_client, base_url, agent_system_prompt):`
   - Each test: navigate to starting URL via `agent_backend.page.goto(url, timeout=60000)` then `agent_backend.page.wait_for_load_state("networkidle")`, then call `run_agent_scenario(...)` with a specific goal, then assert `result["verdict"] != "FAIL"` with descriptive failure message including `result["steps"]` and `result["reasoning"]`.

   Test functions:

   a. `test_auth_login_page_agent` — `@pytest.mark.req("AIQA-04")`, `@pytest.mark.ai_agent`
      - Navigate to `{base_url}/login`
      - Goal: "You are on the GatherGood login page. Verify: 1) There is an email input field. 2) There is a password input field. 3) There is a Login/Sign In submit button. 4) There is a link to the registration page. Report PASS if all four elements are visible, FAIL otherwise."
      - max_iterations=10 (simple page, few steps needed)

   b. `test_auth_register_page_agent` — `@pytest.mark.req("AIQA-04")`, `@pytest.mark.ai_agent`
      - Navigate to `{base_url}/register` (or `/signup` — use `/register` first, if 404 try `/signup`)
      - Goal: "You are on the GatherGood registration page. Verify: 1) There are input fields for email, password, first name, and last name. 2) There is a Register/Sign Up submit button. 3) There is a link back to the login page. Report PASS if all elements are present, FAIL otherwise."
      - max_iterations=10

   c. `test_auth_login_flow_agent` — `@pytest.mark.req("AIQA-04")`, `@pytest.mark.ai_agent`
      - Navigate to `{base_url}/login`
      - Use `from factories.common import unique_email` and `from settings import Settings` to get API_URL
      - Before the agent runs, use httpx to register a test user (email=unique_email(), password="AgentTest123!") via POST to `{api_url}/auth/register/`
      - Goal: "You are on the GatherGood login page. Type the email '{email}' into the email field and '{password}' into the password field. Click the login button. After login, verify that the page navigates away from /login and shows a dashboard or home page with user-specific content (like a navigation menu with Dashboard or profile links). Report PASS if login succeeds and you see authenticated content, FAIL if login fails or you remain on the login page."
      - max_iterations=15

3. Create `tests/ai_agents/scenarios/test_event_management_agent.py` with:
   - Uses httpx to create prerequisite data via API (register user, create org, create event, publish it) before handing off to the agent for browser verification. Import `httpx`, `from factories.common import unique_email, org_name, event_title`, `from helpers.api import assert_status`, `from settings import Settings`.

   Test functions:

   a. `test_event_public_page_agent` — `@pytest.mark.req("AIQA-05")`, `@pytest.mark.ai_agent`
      - API setup: register user, login, create org, create event with title, create free tier, publish event
      - Navigate agent to `{base_url}/events` (or the public events browse page)
      - Goal: "You are on the GatherGood events listing page. Verify: 1) The page shows a list or grid of events. 2) Look for an event with a title containing '{unique_title_substring}'. 3) If found, click on it to view the event detail page. 4) On the detail page, verify there is event information (title, date, description area) and a ticket/registration section. Report PASS if you can browse events and view event details, FAIL otherwise."
      - max_iterations=15

   b. `test_event_dashboard_agent` — `@pytest.mark.req("AIQA-05")`, `@pytest.mark.ai_agent`
      - API setup: register user, login to get tokens
      - Navigate agent to `{base_url}/login`, have agent log in, then navigate to dashboard
      - Goal: "You are on the GatherGood login page. Log in with email '{email}' and password '{password}'. After login, navigate to the dashboard. Verify: 1) You can see a dashboard page with organization or event management options. 2) There are options to create or manage events. Report PASS if the dashboard is accessible and shows management features, FAIL otherwise."
      - max_iterations=18
  </action>
  <verify>
    <automated>cd /Users/user/Test\ event\ planning\ website && python -c "
import ast, sys

# Check AST structure
for f in ['tests/ai_agents/scenarios/test_auth_agent.py', 'tests/ai_agents/scenarios/test_event_management_agent.py']:
    with open(f) as fh:
        tree = ast.parse(fh.read())
    funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    print(f'{f}: {funcs}')
    if not funcs:
        print(f'ERROR: no test functions in {f}')
        sys.exit(1)
print('AST OK')
" && PYTHONPATH=/Users/user/Test\ event\ planning\ website python -c "
# Verify actual imports resolve (not just AST)
from tests.ai_agents.scenarios import test_auth_agent
from tests.ai_agents.scenarios import test_event_management_agent
print('Import OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - tests/ai_agents/scenarios/__init__.py exists
    - tests/ai_agents/scenarios/test_auth_agent.py contains `def test_auth_login_page_agent(`
    - tests/ai_agents/scenarios/test_auth_agent.py contains `def test_auth_register_page_agent(`
    - tests/ai_agents/scenarios/test_auth_agent.py contains `def test_auth_login_flow_agent(`
    - tests/ai_agents/scenarios/test_auth_agent.py contains `@pytest.mark.req("AIQA-04")`
    - tests/ai_agents/scenarios/test_auth_agent.py contains `@pytest.mark.ai_agent`
    - tests/ai_agents/scenarios/test_event_management_agent.py contains `def test_event_public_page_agent(`
    - tests/ai_agents/scenarios/test_event_management_agent.py contains `def test_event_dashboard_agent(`
    - tests/ai_agents/scenarios/test_event_management_agent.py contains `@pytest.mark.req("AIQA-05")`
    - All test functions call `run_agent_scenario`
    - All test functions assert `result["verdict"] != "FAIL"`
    - `PYTHONPATH=. python -c "from tests.ai_agents.scenarios import test_auth_agent"` exits 0
  </acceptance_criteria>
  <done>Auth (3 tests) and event management (2 tests) agent scenarios created with req markers and ai_agent markers</done>
</task>

<task type="auto">
  <name>Task 2: Create checkout, check-in, and permissions agent scenarios</name>
  <files>tests/ai_agents/scenarios/test_checkout_agent.py, tests/ai_agents/scenarios/test_checkin_agent.py, tests/ai_agents/scenarios/test_permissions_agent.py</files>
  <read_first>
    - /Users/user/Test event planning website/tests/ai_agents/agent_runner.py
    - /Users/user/Test event planning website/tests/ai_agents/conftest.py
    - /Users/user/Test event planning website/tests/ui/conftest.py
    - /Users/user/Test event planning website/.planning/phases/06-ai-qa-agents/06-RESEARCH.md
  </read_first>
  <action>
1. Create `tests/ai_agents/scenarios/test_checkout_agent.py` with:
   - API setup at module level or in fixtures: register user, create org, create published event with free tier (reuse pattern from test_event_management_agent.py)
   - Import `httpx`, `pytest`, `from factories.common import unique_email, org_name, event_title, tier_name`, `from helpers.api import assert_status`, `from settings import Settings`, `from tests.ai_agents.agent_runner import run_agent_scenario`

   Test functions:

   a. `test_checkout_flow_agent` — `@pytest.mark.req("AIQA-06")`, `@pytest.mark.ai_agent`
      - API setup: register user, login, create org, published event, free tier
      - Navigate agent to the public event detail page: `{base_url}/events/{org_slug}/{event_slug}` (or the known URL pattern from existing UI tests)
      - Goal: "You are on a GatherGood event detail page. Verify the checkout flow: 1) Find and click a 'Get Tickets', 'Register', or 'Buy Tickets' button. 2) On the checkout page, verify there are step indicators showing the checkout progress (like 'Select Tickets', 'Your Details', 'Payment', 'Confirmation'). 3) Select 1 ticket from the available tier. 4) Proceed through the checkout steps — this is a free event so no payment is needed. 5) Fill in billing/contact details if prompted (use name 'Agent Tester', email 'agent@test.invalid', phone '555-0100'). 6) Complete the checkout and verify you reach a confirmation page showing a confirmation code. Report PASS if you complete the full checkout and see a confirmation code, FAIL if any step fails, INCONCLUSIVE if you get stuck."
      - max_iterations=20

   b. `test_checkout_page_structure_agent` — `@pytest.mark.req("AIQA-06")`, `@pytest.mark.ai_agent`
      - Same API setup
      - Navigate to event page, have agent click through to checkout
      - Goal: "You are on a GatherGood event page. Click to start the checkout/ticket purchase flow. Once on the checkout page, verify: 1) There are step indicators (numbered steps like 1, 2, 3, 4 or labeled steps). 2) The current step is highlighted differently from future steps. 3) There is a ticket selection area with quantity controls. Report PASS if the checkout page has clear step progression and ticket selection, FAIL otherwise."
      - max_iterations=15

2. Create `tests/ai_agents/scenarios/test_checkin_agent.py` with:
   - API setup: register user, login, create org, published event, free tier, complete a free checkout via API to get a ticket, then navigate to check-in page

   Test functions:

   a. `test_checkin_page_agent` — `@pytest.mark.req("AIQA-07")`, `@pytest.mark.ai_agent`
      - API setup: full checkout flow via API (create org, event, tier, publish, checkout with free tier)
      - Navigate agent to the check-in management page. The URL pattern is likely `{base_url}/dashboard/organizations/{org_slug}/events/{event_slug}/check-in` or similar. If unknown, have the agent log in and navigate from dashboard.
      - Goal: "You are logged in to GatherGood. Navigate to the check-in page for the event (look for 'Check-in' or 'Check In' in the event management area). Verify: 1) There is a QR scanner section or camera/scan button. 2) There is a search field to look up attendees by name, email, or confirmation code. 3) There are check-in statistics showing counts (total registered, checked in, etc.). Report PASS if all three elements are present, FAIL if any are missing, INCONCLUSIVE if you cannot find the check-in page."
      - max_iterations=18

3. Create `tests/ai_agents/scenarios/test_permissions_agent.py` with:

   Test functions:

   a. `test_permission_boundary_volunteer_agent` — `@pytest.mark.req("AIQA-08")`, `@pytest.mark.ai_agent`
      - API setup: register OWNER user, create org, invite a VOLUNTEER user (register second user, invite via API), login VOLUNTEER
      - Navigate agent to `{base_url}/login`, log in as VOLUNTEER
      - Goal: "You are logged in as a VOLUNTEER member of a GatherGood organization. Navigate to the organization dashboard. Verify: 1) You can see the organization page. 2) You do NOT see options to create new events (no 'Create Event' button, or it is disabled/hidden). 3) You do NOT see options to invite/manage team members. Report PASS if the volunteer role correctly restricts event creation and team management, FAIL if you can access these restricted features, INCONCLUSIVE if you cannot determine the restrictions."
      - max_iterations=18

   b. `test_permission_boundary_non_member_agent` — `@pytest.mark.req("AIQA-08")`, `@pytest.mark.ai_agent`
      - API setup: register OWNER user, create org; register a separate non-member user
      - Navigate agent as non-member
      - Goal: "You are logged in as a user who is NOT a member of any organization. Try to access an organization's dashboard or management pages. Verify: 1) You cannot see other organizations' private dashboards. 2) Attempting to access management URLs shows an error, redirect, or empty state. Report PASS if non-members are properly restricted, FAIL if you can access organization management features."
      - max_iterations=15
  </action>
  <verify>
    <automated>cd /Users/user/Test\ event\ planning\ website && python -c "
import ast, sys
files = [
    'tests/ai_agents/scenarios/test_checkout_agent.py',
    'tests/ai_agents/scenarios/test_checkin_agent.py',
    'tests/ai_agents/scenarios/test_permissions_agent.py',
]
total = 0
for f in files:
    with open(f) as fh:
        tree = ast.parse(fh.read())
    funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    total += len(funcs)
    print(f'{f}: {funcs}')
    if not funcs:
        print(f'ERROR: no test functions in {f}')
        sys.exit(1)
print(f'Total test functions: {total}')
print('AST OK')
" && PYTHONPATH=/Users/user/Test\ event\ planning\ website python -c "
# Verify actual imports resolve (not just AST)
from tests.ai_agents.scenarios import test_checkout_agent
from tests.ai_agents.scenarios import test_checkin_agent
from tests.ai_agents.scenarios import test_permissions_agent
print('Import OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - tests/ai_agents/scenarios/test_checkout_agent.py contains `def test_checkout_flow_agent(`
    - tests/ai_agents/scenarios/test_checkout_agent.py contains `def test_checkout_page_structure_agent(`
    - tests/ai_agents/scenarios/test_checkout_agent.py contains `@pytest.mark.req("AIQA-06")`
    - tests/ai_agents/scenarios/test_checkin_agent.py contains `def test_checkin_page_agent(`
    - tests/ai_agents/scenarios/test_checkin_agent.py contains `@pytest.mark.req("AIQA-07")`
    - tests/ai_agents/scenarios/test_permissions_agent.py contains `def test_permission_boundary_volunteer_agent(`
    - tests/ai_agents/scenarios/test_permissions_agent.py contains `def test_permission_boundary_non_member_agent(`
    - tests/ai_agents/scenarios/test_permissions_agent.py contains `@pytest.mark.req("AIQA-08")`
    - All test files contain `@pytest.mark.ai_agent`
    - All test functions call `run_agent_scenario`
    - All test functions assert on `result["verdict"]`
    - `PYTHONPATH=. python -c "from tests.ai_agents.scenarios import test_checkout_agent"` exits 0
  </acceptance_criteria>
  <done>Checkout (2 tests), check-in (1 test), and permissions (2 tests) agent scenarios created with req and ai_agent markers</done>
</task>

</tasks>

<verification>
1. `pytest tests/ai_agents/ --collect-only` collects all 10 test functions from 5 scenario files
2. Each test file contains `@pytest.mark.req("AIQA-0X")` marker
3. Each test file contains `@pytest.mark.ai_agent` marker
4. Each test function calls `run_agent_scenario` from agent_runner
5. Without ANTHROPIC_API_KEY, `pytest tests/ai_agents/ -v` shows all tests as SKIPPED with "ANTHROPIC_API_KEY not configured" message
</verification>

<success_criteria>
- 5 scenario test files exist in tests/ai_agents/scenarios/
- 10 total test functions covering auth, event management, checkout, check-in, permissions
- All tests have AIQA-XX requirement markers
- All tests use the agent_backend and claude_client fixtures
- Tests skip cleanly without API key, run with API key
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-qa-agents/06-02-SUMMARY.md`
</output>
