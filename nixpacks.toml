[phases.setup]
nixPkgs = ["chromium", "xvfb-run"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install chromium --with-deps"]

[start]
cmd = "uvicorn api.persona_runner:app --host 0.0.0.0 --port ${PORT:-8000}"
